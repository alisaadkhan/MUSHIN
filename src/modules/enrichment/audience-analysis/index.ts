// Audience Analysis — demographic breakdown, city / age / gender split
// Loaded on-demand via the Enrich button; not auto-fetched on profile load.
export { useAIInsights } from "@/hooks/useAIInsights";
